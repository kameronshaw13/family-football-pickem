This patch uses the Chargers art only as the installed home-screen/PWA icon.
It removes the icon image from the in-app header and login card.
After deploying, delete the old iPhone home screen shortcut and add it again from Safari so iOS refreshes the cached icon.
